import 'package:flutter/material.dart';

class OsTheme {
  bool isDark = false;
  Color darkTheme = const Color(0XFF1E201E);
  Color lightTemen = Colors.white;
}

class ThemeColor {
  static Color mainBackgroundColor = const Color(0XFF1E201E);
}
